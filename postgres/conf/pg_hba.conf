# postgresql/pg_hba.conf
# Allow all connections from within the Docker network (no SSL).
# Host    database    user    address         auth-method
host    all         all     0.0.0.0/0       md5
host    all         all     ::/0            md5
local   all         all                     trust
